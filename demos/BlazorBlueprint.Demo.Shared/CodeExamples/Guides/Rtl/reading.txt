@* Resolve falls back to the current culture when there is no provider. *@
<div class="@(IsRtl ? "text-end" : "text-start")">@Label</div>

@code {
    [CascadingParameter] private DirectionContext? Direction { get; set; }

    private bool IsRtl => DirectionContext.Resolve(Direction);
}

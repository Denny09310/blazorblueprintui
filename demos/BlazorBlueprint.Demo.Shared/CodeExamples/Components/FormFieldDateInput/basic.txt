<BbFormFieldDateInput @bind-Value="startDate"
                      Label="Start date"
                      HelperText="Type each part, or use the arrow keys." />

@code {
    private DateTime? startDate;
}

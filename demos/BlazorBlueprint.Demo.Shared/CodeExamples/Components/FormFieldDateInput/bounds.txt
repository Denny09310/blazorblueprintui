@* MinDate and MaxDate are inclusive. Out-of-range parts are rejected as you type. *@
<BbFormFieldDateInput @bind-Value="bookingDate"
                      Label="Booking date"
                      HelperText="Within the next 30 days."
                      MinDate="DateTime.Today"
                      MaxDate="DateTime.Today.AddDays(30)"
                      ShowCalendar="true" />

@code {
    private DateTime? bookingDate;
}

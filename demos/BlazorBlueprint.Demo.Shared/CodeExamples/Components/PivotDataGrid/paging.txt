@* PageSize counts outermost row groups, not rows, so a group is never split across a
   boundary and its subtotal always lands with it. Height makes the headings stay put
   while the body scrolls. *@
<BbPivotDataGrid TItem="Sale" Data="@sales"
                 Totals="PivotTotals.All"
                 PageSize="2"
                 PagerPosition="PivotPagerPosition.Bottom"
                 Height="22rem"
                 GridLines="PivotGridLines.Horizontal">
    ...
</BbPivotDataGrid>

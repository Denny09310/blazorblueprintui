<BbPickList TValue="string"
            Options="@options"
            @bind-Values="picked"
            OnMove="HandleMove" />

@code {
    private void HandleMove(PickListMoveEventArgs<string> e)
    {
        var direction = e.ToTarget ? "picked" : "returned";
        Console.WriteLine($"{e.Values.Count} {direction}");
    }
}

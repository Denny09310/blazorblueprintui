@* A vertical slider runs bottom to top. Give it a height — it fills its container. *@
<div class="h-48">
    <BbSlider @bind-Value="verticalValue"
              Orientation="SliderOrientation.Vertical" />
</div>

@code {
    private double verticalValue = 60;
}

@* OptionDisabled takes the option, so the rule can use the value or the text. *@
<BbListBox TValue="string"
           Options="@options"
           OptionDisabled="@(o => soldOut.Contains(o.Value))"
           @bind-Value="choice" />

@code {
    private readonly HashSet<string> soldOut = ["express"];
}

@* The selected indicator is drawn either way; the template owns the rest of the row. *@
<BbListBox TValue="string" Options="@options" @bind-Value="choice">
    <ItemTemplate Context="option">
        <span class="flex-1">@option.Text</span>
        <span class="text-xs text-muted-foreground">@option.Value</span>
    </ItemTemplate>
</BbListBox>

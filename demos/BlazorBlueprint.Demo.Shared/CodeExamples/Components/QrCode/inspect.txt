@* Debounced rather than Immediate: the code is cheap to build, but a Blazor Server
   round trip per keystroke is not. *@
<BbInput @bind-Value="value"
         UpdateTiming="UpdateTiming.Debounced"
         DebounceInterval="250" />

@* OnEncoded, not @ref and Matrix: a parent builds its markup before its children
   re-render, so reading Matrix here would show the previous grid. *@
<BbQrCode Value="@value" OnEncoded="@(m => matrix = m)" Size="14rem" />

@if (matrix is { } code)
{
    <p>
        @code.Mode, version @code.Version
        (@code.Size &times; @code.Size modules), mask @code.Mask
    </p>
}

@code {
    private string value = "https://blazorblueprintui.com";
    private QrMatrix? matrix;
}

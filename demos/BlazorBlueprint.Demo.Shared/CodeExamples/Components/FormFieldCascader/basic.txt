<BbFormFieldCascader TItem="Node"
                     @bind-Value="selected"
                     Label="Category"
                     HelperText="Navigate the columns, or search the full path."
                     Items="categories"
                     ValueField="n => n.Id"
                     TextField="n => n.Name"
                     ChildrenProperty="n => n.Children" />

@code {
    private string? selected;
    private readonly List<Node> categories = [ /* … */ ];

    private sealed record Node(string Id, string Name, List<Node>? Children);
}

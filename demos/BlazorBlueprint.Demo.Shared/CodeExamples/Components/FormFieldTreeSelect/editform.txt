@* Bind the expression that matches the mode you are in: Value for single
   selection, Values for multiple. The validation message follows it. *@
<EditForm Model="model" OnValidSubmit="HandleSubmit">
    <DataAnnotationsValidator />
    <BbFormFieldTreeSelect TItem="Node"
                           @bind-Value="model.DepartmentId"
                           Label="Department"
                           HelperText="Required."
                           Required="true"
                           Items="departments"
                           ValueField="n => n.Id"
                           TextField="n => n.Name"
                           ChildrenProperty="n => n.Children" />
    <BbButton Type="ButtonType.Submit">Submit</BbButton>
</EditForm>

@code {
    private readonly Model model = new();

    private sealed class Model
    {
        [Required(ErrorMessage = "Choose a department.")]
        public string? DepartmentId { get; set; }
    }
}

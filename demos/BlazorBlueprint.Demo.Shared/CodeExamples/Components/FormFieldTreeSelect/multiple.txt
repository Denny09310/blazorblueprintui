@* Multiple selection binds Values. Ticking a parent cascades to its children. *@
<BbFormFieldTreeSelect TItem="Node"
                       @bind-Values="selected"
                       Label="Teams"
                       HelperText="Pick one or more teams."
                       Multiple="true"
                       LeafOnly="true"
                       Items="departments"
                       ValueField="n => n.Id"
                       TextField="n => n.Name"
                       ChildrenProperty="n => n.Children" />

@code {
    private HashSet<string> selected = [];
}

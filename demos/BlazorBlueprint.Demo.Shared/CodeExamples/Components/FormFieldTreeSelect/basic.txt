<BbFormFieldTreeSelect TItem="Node"
                       @bind-Value="selected"
                       Label="Department"
                       HelperText="Search, or expand a branch to pick a team."
                       Items="departments"
                       ValueField="n => n.Id"
                       TextField="n => n.Name"
                       ChildrenProperty="n => n.Children" />

@code {
    private string? selected;
    private readonly List<Node> departments = [ /* … */ ];

    private sealed record Node(string Id, string Name, List<Node>? Children);
}

@* A vertical slider runs bottom to top. Give it a height — it fills its container. *@
<div class="h-48">
    <BbRangeSlider @bind-Value="verticalRange"
                 Orientation="RangeSliderOrientation.Vertical"
                 ShowTooltips="true" />
</div>

@code {
    private (double Start, double End) verticalRange = (20, 80);
}

@* Dark on white in both themes. A scanner measures the contrast between a bar and
   the space beside it, and an inverted symbol reads as no symbol at all on most
   hardware. Change these only where the result will not be scanned. *@
<BbBarcode Value="BB-2026-0917" />

<BbBarcode Value="BB-2026-0917" Foreground="#1e3a8a" Background="#eff6ff" />

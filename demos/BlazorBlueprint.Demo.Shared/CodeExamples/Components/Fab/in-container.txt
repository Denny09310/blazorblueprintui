@* Fixed="false" pins the button to the nearest positioned ancestor, not the viewport. *@
<div class="relative h-64 rounded-lg border">
    <BbFab Fixed="false" AriaLabel="Add photo" OnClick="Upload">
        <Icon><LucideIcon Name="image-plus" Size="24" /></Icon>
    </BbFab>
</div>

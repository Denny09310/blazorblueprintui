@foreach (var label in labels)
{
    <BbChip @key="label" Dismissible="true" OnDismiss="@(() => labels.Remove(label))">
        @label
    </BbChip>
}

@code {
    private readonly List<string> labels = ["Design", "Build", "Ship"];
}

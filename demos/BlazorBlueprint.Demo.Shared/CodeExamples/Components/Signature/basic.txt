<BbSignature Label="Signature" @bind-Value="signature" />

@if (signature is { IsEmpty: false })
{
    <p>Signed by @(signature.Kind == SignatureKind.Typed ? "typing" : "drawing").</p>
}

@code {
    private SignatureValue? signature;
}

@namespace BlazorBlueprint.Primitives

@* Renders all registered portals at the document body level.
   Composes both category hosts: Container (Dialog, Sheet) and Overlay (Popover, Select, etc.).
   For category-scoped rendering, use BbContainerPortalHost and BbOverlayPortalHost directly. *@
<BbContainerPortalHost />
<BbOverlayPortalHost />

@namespace BlazorBlueprint.Components

@if (ChildContent is not null && index >= 0 && index == Stepper?.ActiveStepIndex)
{
    <div class="@CssClass" data-slot="step" @attributes="AdditionalAttributes">
        @ChildContent
    </div>
}

@namespace BlazorBlueprint.Components

@foreach (var segment in Segments)
{
    @if (segment.IsMatch)
    {
        <mark class="@MarkCssClass">@segment.Text</mark>
    }
    else
    {
        @segment.Text
    }
}
